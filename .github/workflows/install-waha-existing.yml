name: WAHA metadata installer — superseded

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/install-waha-existing.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: nitu-waha-existing-vm-install
  cancel-in-progress: true

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'This metadata-image installer was superseded by the reversible Oracle boot-volume replacement workflow.'
          echo 'No infrastructure or container change is performed here.'
