name: Legacy Run Command recovery (disabled)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deprecated:
    runs-on: ubuntu-latest
    steps:
      - name: Explain supported recovery path
        run: |
          echo 'The legacy Oracle Run Command recovery is intentionally disabled.'
          echo 'It previously remained in ACCEPTED state without executing on the instance.'
          echo 'Use the Control-plane WhatsApp recovery workflow instead.'
