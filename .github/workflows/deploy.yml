name: Legacy custom gateway deployment — retired

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy deployment disabled
        run: |
          echo 'The custom Baileys/Chromium gateway deployment is retired.'
          echo 'Production transport is managed by the WAHA Core NOWEB Oracle workflow.'
          echo 'This workflow intentionally performs no infrastructure or container changes.'
