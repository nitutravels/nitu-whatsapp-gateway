name: Oracle-agent WAHA resume — superseded

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - '.github/workflows/resume-waha-after-replacement.yml'

permissions:
  contents: read

concurrency:
  group: nitu-waha-resume-after-replacement
  cancel-in-progress: true

jobs:
  superseded:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'The Oracle Run Command plugin is unavailable on this image.'
          echo 'This workflow is superseded by the cloud-init boot-volume installation and performs no changes.'
