name: Install WAHA on Oracle

on:
  workflow_dispatch:
  push:
    branches: [main]

permissions:
  contents: write
  packages: read

concurrency:
  group: nitu-whatsapp-waha-install
  cancel-in-progress: false

jobs:
  install:
    if: github.event_name == 'workflow_dispatch' || contains(github.event.head_commit.message, '[waha-deploy]')
    runs-on: ubuntu-latest
    environment: production
    timeout-minutes: 60

    steps:
      - uses: actions/checkout@v7

      - uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Configure OCI credentials
        shell: bash
        env:
          OCI_PRIVATE_KEY_PEM: ${{ secrets.OCI_PRIVATE_KEY }}
          OCI_FINGERPRINT_VALUE: ${{ secrets.OCI_FINGERPRINT }}
        run: |
          set -Eeuo pipefail
          mkdir -p "$HOME/.oci"
          python - <<'PY'
          import os
          from pathlib import Path
          value = os.environ.get('OCI_PRIVATE_KEY_PEM', '').strip()
          if len(value) >= 2 and value[0] == value[-1] and value[0] in {'"', "'"}:
              value = value[1:-1]
          if '\\n' in value and '\n' not in value:
              value = value.replace('\\r', '').replace('\\n', '\n')
          Path.home().joinpath('.oci/key.pem').write_text(value.replace('\r', '').strip() + '\n', encoding='utf-8')
          PY
          chmod 600 "$HOME/.oci/key.pem"
          fingerprint="$(printf '%s' "$OCI_FINGERPRINT_VALUE" | tr -d '[:space:]')"
          cat > "$HOME/.oci/config" <<EOF
          [DEFAULT]
          user=${{ secrets.OCI_USER_OCID }}
          fingerprint=$fingerprint
          tenancy=${{ secrets.OCI_TENANCY_OCID }}
          region=${{ vars.OCI_REGION }}
          key_file=$HOME/.oci/key.pem
          EOF
          chmod 600 "$HOME/.oci/config"

      - name: Install deployment utilities
        shell: bash
        run: |
          set -Eeuo pipefail
          pipx install --quiet oci-cli
          sudo apt-get update -qq
          sudo apt-get install -y -qq dnsutils

      - name: Install WAHA Core NOWEB through OCI control plane
        shell: bash
        env:
          COMPARTMENT_OCID: ${{ vars.OCI_COMPARTMENT_OCID }}
          DOMAIN: ${{ vars.GATEWAY_DOMAIN || 'wa.nitutravels.in' }}
          API_KEY: ${{ secrets.GATEWAY_API_KEY }}
          IMAGE: ghcr.io/${{ github.repository }}:${{ github.sha }}
          SESSION_NAME: nitu-travels
        run: |
          set -Eeuo pipefail
          mkdir -p docs
          chmod 0755 scripts/deploy-waha.sh
          scripts/deploy-waha.sh 2>&1 | tee docs/waha-deployment-result.txt

      - name: Persist sanitized deployment result
        if: always()
        shell: bash
        run: |
          set -Eeuo pipefail
          test -f docs/waha-deployment-result.txt || \
            echo 'WAHA installation did not produce a result file.' > docs/waha-deployment-result.txt
          git config user.name 'github-actions[bot]'
          git config user.email '4189826+github-actions[bot]@users.noreply.github.com'
          git add docs/waha-deployment-result.txt
          git diff --cached --quiet || git commit -m 'Record WAHA Oracle installation result'
          for attempt in 1 2 3 4 5; do
            git pull --rebase origin main && git push origin HEAD:main && exit 0
            sleep $((attempt * 5))
          done
          echo 'Could not persist result to main; installation logs remain in this workflow.' >&2
          exit 1
